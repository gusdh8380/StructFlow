# StructFlow EC2 환경 변수 템플릿
# EC2에서 이 파일을 .env로 복사 후 값을 채워서 사용:
#   cp .env.example .env && nano .env

# Docker 이미지 이름 (로컬 빌드 시 기본값 사용 가능)
SIMULATION_IMAGE=structflow-api:latest

# n8n 설정
N8N_HOST=<EC2_PUBLIC_IP_OR_DOMAIN>       # 예: 54.123.45.67
N8N_PROTOCOL=http
N8N_WEBHOOK_URL=http://<EC2_PUBLIC_IP>/webhook/
N8N_ENCRYPTION_KEY=<랜덤_32자_문자열>    # openssl rand -hex 32

# n8n 관리 UI 로그인
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=<강력한_비밀번호>
